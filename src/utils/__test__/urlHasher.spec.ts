import { UrlHasher } from '../urlHasher';

describe('Testing UrlHasher class', () => {
  // it('Url must return a string', () => {
  //   const string: string = 'https://alexiswebsite.com';
  //   const response: string = UrlHasher.hash(string);
  //   expect(typeof response).toBe('string');
  // });
});
